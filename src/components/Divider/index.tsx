import React from 'react';

import { Container, DividerContent } from './styles';

const Divider: React.FC = () => {
  return (
    <Container>
      <DividerContent />
    </Container>
  );
};

export default Divider;
